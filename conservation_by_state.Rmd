---
title: "Untitled"
output: 
  flexdashboard::flex_dashboard:
    orientation: columns
    vertical_layout: fill
---

```{r setup, include=FALSE}
library(flexdashboard)
```

Column {data-width=650}
-----------------------------------------------------------------------

### Amount of substantive restrictions by state

```{r}
library(ggplot2)
library(tidyverse)
library(urbnmapr)
conservation <- readRDS("states_es_laws.rds", refhook = NULL)

state_data <- left_join(conservation, states, by = c("state" = "state_name")) 

state_data %>%
  ggplot(aes(long, lat, group = group, fill = substantive_restrictions)) +
   geom_polygon(color = "#ffffff", size = .15) +
  coord_map(projection = "albers", lat0 = 39, lat1 = 45) +
  
  labs(fill = "Substantive Restrictions")
```

Column {data-width=350}  {data-height=700}
-----------------------------------------------------------------------

### Chart B

```{r}
ggplot(data = conservation, aes(x = conserv_programs, y = code)) +
  geom_point()

```

### Chart C

```{r}

```

